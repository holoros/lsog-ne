% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{threshold_provenance}
\alias{threshold_provenance}
\title{Provenance for one axis or one instrument}
\usage{
threshold_provenance(instrument, axis = NULL)
}
\arguments{
\item{instrument}{character, one instrument name as accepted by
\code{lsog_thresholds}}

\item{axis}{optional character, one or more axis names within that
instrument, for example \code{"s_dw"} or \code{"sc_dead"}}
}
\value{
a data frame with columns \code{instrument}, \code{axis},
\code{jurisdiction}, \code{source_file}, \code{source_line}, and
\code{note}
}
\description{
Returns the source file, source line, and note for every threshold row of
one instrument, optionally narrowed to one axis. This is the function to
call when the question is where a number came from rather than what it is.
}
\examples{
threshold_provenance("nb_v51", "sc_dead")
threshold_provenance("strict4")
}
